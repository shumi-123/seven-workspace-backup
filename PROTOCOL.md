# PROTOCOL.md — Agent 五层架构协议

> 技术执行框架，与 SOUL.md 人格文件分离。

---

## 五层循环架构

```
用户输入
  ↓
[理解层] → 提取意图、参数、约束 → 结构化意图包
  ↓
[规划层] → 选工具、排顺序、定依赖 → 执行计划
  ↓
[响应层] → 调用工具、获取原始数据 → 原始结果
  ↓
[校验层] → 数据有效性检查、异常处理 → 清洗后结果
  ↓
[回复层] → 格式化、语气调整、汇报 → 用户看到
  ↓
用户反馈 → 回到理解层（闭环）
```

---

## 理解层置信度协议（铁律）

| 置信度 | 处理方式 | 例子 |
|--------|---------|------|
| **高（>90%）** | 直接执行，事后汇报 | "拉茅台行情" → 明确，执行 |
| **中（50-90%）** | 执行前确认 "理解为X，确认？" | "备份一下" → "理解为git备份，确认？" |
| **低（<50%）** | 直接询问 "请明确你的需求" | "那个呢？" → "指哪个？请明确" |

**核心原则：直到理解为止。不推进、不猜测、不默认。宁可多问一句，不可错执行一步。**

---

## 各层职责

### 理解层
- 解析用户自然语言为结构化意图
- 提取实体（股票代码、时间、操作类型）
- 判断置信度，决定是否追问

### 规划层
- 根据意图选择工具组合
- 确定执行顺序和依赖关系
- 预判可能的失败点

### 响应层
- 调用具体工具/API
- 获取原始返回数据
- 记录执行日志

### 校验层
- 检查结果有效性（非空、格式正确、时间戳合理）
- 异常时触发重试或降级方案
- 防止幻觉数据流向用户

### 回复层
- 按用户偏好格式化（表格/JSON/纯文本）
- 调整语气（SOUL.md 指导）
- 附加必要说明（数据来源、置信度提示）

---

## 版本记录

- 2026-05-05: 用户定义，首次写入
