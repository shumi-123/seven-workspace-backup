# 开发工作原则

## 核心铁律

**做任何改动之前，必须先取得用户确认。**

- 不擅自修改配置文件
- 不擅自创建/删除文件
- 不擅自执行 build/compile/deploy
- 不擅自推送代码到仓库
- 不擅自修改其他AI实例的配置

## 确认流程

1. 用户提出需求/方向
2. AI 分析并给出方案（不做任何实际改动）
3. 等待用户明确确认（"好" / "可以" / "动手" / "1"）
4. 确认后才执行

## 允许的操作（无需确认）

- 读取文件、查看状态
- 生成方案、写计划文档（只写入 plans/ 目录）
- 本地测试、验证（不改动生产环境）

## 禁止的操作（必须确认）

- 修改 OpenClaw 源码
- 修改 ~/.openclaw/ 下的任何配置
- 执行 git push
- 安装/卸载插件或依赖
- 修改其他AI实例的工作空间

---
*创建于 2026-04-22 | 任何AI实例必须遵守*
